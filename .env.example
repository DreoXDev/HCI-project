# Optional local environment overrides.
# Copy to `.env` only for local experiments; `.env` is ignored by Git.

# HCI_CONFIG=config.yaml
# LIBREOFFICE_PATH=C:/Program Files/LibreOffice/program/soffice.exe
